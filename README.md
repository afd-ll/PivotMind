# 玄枢 (PivotMind) — 溯智系统

**纯C实现的认知AI框架**，基于多拓扑网络的自主学习和推理引擎。

47个源文件，约5.4万行C代码，全部手写，无第三方AI框架依赖。

## 重要说明

> ⚠️ **这不是开箱即用的聊天机器人。**
>
> 这是一个**认知架构研究框架**，核心机制是多拓扑网络+在线持续学习。
> 对话能力是从零开始、通过交互逐步成长的——刚开始只会说"我想，联想到【X】、【Y】。"
> 聊得越多，句型和知识越丰富。系统设计为7×24小时运行，与它对话就是训练它。

## 支持平台

| 平台 | 状态 | 说明 |
|------|------|------|
| Linux | ✅ 支持 | 主目标平台，树莓派3B实测通过 |
| ARM Linux (树莓派) | ✅ 支持 | 905Mi内存 + 4核Cortex-A53 实测运行 |
| x86 Linux | ✅ 支持 | 标准GCC编译 |
| macOS | ⚠️ 未测试 | 理论上可编译，需验证 |
| Windows (WSL) | ⚠️ 需WSL | 原生Win32支持已移除(遗留代码已清理) |
| Windows (原生) | ❌ 不支持 | 旧Windows代码已删除 |

**硬件限制：**
- 最低内存: 512MB（编译需~1.5GB，建议使用swap）
- 磁盘: 编译产物约500KB，运行时约50MB（拓扑数据）
- 主动学习器后台线程会定期执行计算密集型操作（PageRank、EWC等），低功耗设备上可能影响响应速度

## 架构概览

```
输入 → 语义理解 → 多拓扑联想推理 → 模板组合 → 回复
         ↓                      
     auto_learn_concepts() ← 在线学习概念
         ↓
     记忆系统 (上下文/短期/永久)
         ↓
     主动学习器 (后台线程每5分钟)
         ├─ 节点重要性评估 (PageRank)
         ├─ 共现关系发现
         ├─ 拓扑自动增长/收缩
         ├─ 遗忘机制 (低置信度清理)
         └─ EWC持续学习巩固
```

## 核心组件

| 模块 | 功能 | 行数 |
|------|------|------|
| 多拓扑网络 | 8层概念拓扑（词汇/语义/情绪/语法/上下文/领域/语用/文化） | ~1500 |
| 华容道拓扑 | 推理节点 + 联想传播 + 激活扩散 | ~1500 |
| 对话系统 | 意图识别 + 实体提取 + 拓扑驱动回复生成 | ~1700 |
| 主动学习器 | 后台线程持续学习 + 关系发现 + 拓扑增长 | ~800 |
| Seq2Seq模型 | Encoder-Decoder RNN + BPTT在线训练 | ~400 |
| 概念抽象 | 7层概念层次（具体→感觉→空间→时间→分类→因果→意图） | ~400 |
| 因果推理 | 因果关系图 + A*因果路径搜索 | ~2000 |
| 记忆系统 | 三级记忆（上下文/短期/永久） + 遗忘机制 | ~850 |
| 神经网络层 | Embedding / SimpleRNN / Linear (前向+反向) | ~3000 |
| 中文分词 | UTF-8按字符分词（逐字/英文连续） | ~100 |
| 概念处理 | 数学表达式计算 + 规则学习 | ~270 |
| 拓扑持久化 | 二进制序列化保存/加载拓扑状态 | ~150 |

## 快速开始

```bash
# 编译（Linux/树莓派）
make linux

# 运行
./build/bin/digital_life

# 命令
#   help    - 显示帮助
#   quit    - 退出
#   stats   - 学习统计
#   network - 查看拓扑网络
#   memory  - 查看记忆状态
#   learn   - 手动触发学习周期

# 直接对话即可，每次对话都会自动学习
```

## 构建选项

| 命令 | 说明 |
|------|------|
| `make linux` | 全量编译（先清理再构建，约3分钟在树莓派3B上） |
| `make lib` | 仅构建静态库 libpivotmind.a |
| `make run` | 构建并运行 |
| `make clean` | 清理构建产物 |

## 依赖

- GCC/Clang (C99 + GNU扩展 `-std=gnu99`)
- pthread (多线程支持)
- SQLite3 (词典/记忆持久化)
- OpenMP (可选，编译时启用并行)

## 技术特点

- **全C实现** — 无Python/无第三方AI框架依赖，纯手写
- **在线持续学习** — 无需预训练/无需数据集，每轮对话即训练样本
- **概念网络自增长** — 拓扑节点随对话自动扩展（从14个初始节点起步）
- **后台主动学习** — 独立线程每5分钟执行概念巩固/关系发现/遗忘
- **低资源运行** — 树莓派3B (1GB RAM, 4核1.2GHz) 可流畅运行
- **多维度推理** — 词汇/语义/情绪/语法/上下文/领域/语用/文化八层拓扑同时激活
- **状态持久化** — 拓扑网络可保存到文件，重启后恢复（约50KB/2000节点）
- **三权分立架构** — 拓扑网络(联想) + 记忆系统(存储) + 主动学习器(成长) 独立运行

## 项目状态

开发中 — 持续演进。

已完成：
- ✅ 多拓扑认知网络 + 联想推理引擎
- ✅ 对话系统（意图识别 + 实体提取 + 模板回复）
- ✅ 主动学习器 + 拓扑增长/收缩/遗忘
- ✅ 概念层次结构 (7层抽象)
- ✅ 因果推理 (A*路径搜索 + 条件独立性测试)
- ✅ 三级记忆系统 (上下文/短期/永久)
- ✅ 拓扑状态持久化（保存/加载）
- ✅ 节点重要性评估 (PageRank/度中心性)
- ✅ EWC持续学习（费雪信息矩阵）
- ✅ 概念在线学习（从对话中自动提取+建立连接）
- ✅ 概念处理（数学表达式计算/规则学习）
- ✅ Apache 2.0 开源许可证

进行中：
- 🔄 Seq2Seq神经网络在线训练（BPTT反向传播已实现，待集成到回复生成管线）
- 🔄 上下文追踪模块（对话历史跟踪+指代消解）
- 🔄 RNN/LSTM/GRU神经网络层接入推理管线
- 🔄 内存优化（对象池+字符串池）

## 作者

陈道祥

## 许可证

Apache License 2.0。详见 [LICENSE](LICENSE) 文件。

Copyright 2026 陈道祥
