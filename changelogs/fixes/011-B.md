# 修复报告 011-B — 语义拓扑跨拓扑投票接入

> 配对审查报告：011-A
> 日期：2026-05-27

## 一、修复概要

按 011-A 方案，在 `topology_walk_greedy()` 中接入语义拓扑跨拓扑投票。

## 二、已修复项

### 2.1 函数签名扩展 ✅

**文件**：`include/multi_topology.h:289-295`

末尾增加 `MasterTopology* master` 参数。`NULL` 时保持原行为。

### 2.2 函数定义更新 ✅

**文件**：`src/multi_topology.c:1196-1198`

签名与头文件一致。

### 2.3 路径回溯循环增加语义投票 ✅

**文件**：`src/multi_topology.c:1305-1340`

- 新增 `semantic_cross_score`、`semantic_hit_count` 变量
- 在原有词汇拓扑投票基础上，遍历 `master->cross_links` 查找路径节点 → `TOPO_SEMANTIC` 的跨拓扑连接
- 命中时累加 `link->weight`
- 计算 `semantic_cross_norm = semantic_cross_score / semantic_hit_count / 5.0f`

### 2.4 评分公式拆分 ✅

**文件**：`src/multi_topology.c:1353-1355`

```c
// 原：EDGE_WALK_W_PATH_CTX * path_ctx_norm;
// 改：
EDGE_WALK_W_PATH_CTX * path_ctx_norm * 0.6f +
EDGE_WALK_W_PATH_CTX * semantic_cross_norm * 0.4f;
```

### 2.5 三个调用点更新 ✅

| 文件 | 传参 |
|------|------|
| `src/multi_topology.c:1125` | `master` |
| `src/dialog_generate.c:114` | `dsys ? dsys->master : NULL` |
| `src/associative_reasoning.c:385` | `engine->topology` |

## 三、编译结果

✅ `make batch-learn` — 57 个源文件全部编译通过，0 error 0 warning，`build/bin/batch_learn` 生成成功。

## 四、未覆盖项

- 未在测试工具（`tools/test_walk.c` 等）中传 `master`——它们传 `NULL` 走原行为
- 未做语义拓扑的反向映射（查 target 在语义拓扑中的对应节点）
- 未用 `cross_adj` 邻接表优化（当前是 O(n) 线性扫描 cross_links）

## 五、后续待验证

- [ ] 用训练后状态文件跑对话，对比输出质量
- [ ] 检查因果图数据量，决定是否做第二步（因果模块接入）
