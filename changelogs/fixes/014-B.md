# 修复报告 014-B — 全量修复

> 配对审查报告：014-A
> 日期：2026-05-27

## 一、已修复项

### P0：extract_unique_chars → extract_ordered_chars ✅
**文件**：`src/autonomic_learner.c`

去重→保序。已有位置衰减 `wmult = 1.5f/dist` 现在作用在真实位置差上。

---

### P1：topology_walk_beam 完整实现 ✅
**文件**：`include/multi_topology.h` + `src/multi_topology.c` (~215 行)

Beam K=3，全局最优路径选择。3 倍计算换全局视野。

---

### P2：图拉普拉斯平滑特征学习 ✅
**文件**：
- `include/feature_learn.h` — 声明
- `src/feature_learn.c` — 97 行实现
- `src/autonomic_learner.c` — 刷盘前调用

共现邻居加权平均：每轮将每个节点的 features 更新为邻居 features 的加权平均（权重 = 边权 × 置信度），3 轮平滑。features 从"冻结随机数"升级为"编码了共现关系的语义向量"。

**复杂度**：O(iter × edges × 24) ≈ 毫秒级。

---

### P3：三元组链式奖励 ✅
**文件**：`src/multi_topology.c`

走边时动态检查"prev→current→target"两步链，给链式路径 +0.05 额外奖励。

---

## 二、编译结果

✅ `make batch-learn` — 0 error 0 warning。

## 三、改动文件汇总

| 文件 | 改动项 |
|------|--------|
| `src/autonomic_learner.c` | P0 去重→保序 + P2 集成 |
| `include/feature_learn.h` | P2 新建声明 |
| `src/feature_learn.c` | P2 新建实现 |
| `include/multi_topology.h` | P1 Beam 声明 |
| `src/multi_topology.c` | P1 Beam Search + P3 三元组奖励 |
