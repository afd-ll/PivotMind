# 修复报告 012-B — 架构审查全量修复

> 配对审查报告：012-A
> 日期：2026-05-27

## 一、修复概要

按 012-A 审查结果，完成 P0-P2 共 10 项修复。P3 已验证/清理完成。

## 二、已修复项

### P0：语义投票走 cross_adj 邻接表 ✅

**文件**：`src/multi_topology.c:1326-1343`

语义跨拓扑投票从 O(cross_link_count) 线性扫描改为 O(1) cross_adj 邻接表索引。加速约 3-4 个数量级。

---

### P1-4：pruning.c 冒泡排序改快速选择 ✅

**文件**：`src/pruning.c`

- 新增 `quickselect_float()` 辅助函数（Lomuto 分区，O(n) 平均）
- 替换 `prune_tensor_magnitude`、`prune_tensor_gradient`、`prune_structured`（axis=0 和 axis=1）四处冒泡排序

---

### P1-3：递归锁改普通锁 ✅

**文件**：`src/trace_wisdom_topology.c:107-111`

```c
// 改前：PTHREAD_MUTEX_RECURSIVE
// 改后：pthread_mutex_init(&net->mutex, NULL);
```

验证：所有 lock/unlock 成对，无重入调用链。

---

### P1-2：calc_context_activations O(n²) → O(n log n) ✅

**文件**：`src/cognitive_controller.c:116-140`

- token 数组先 qsort 排序
- 内层用 bsearch 精确匹配替代 O(n²) 扫描
- 保留 strstr 反向模糊匹配（概念包含输入）

---

### P1-1：激活传播加 active_set 过滤 ✅

**文件**：`src/dialog_system.c:78-155`

- 传播前先扫描收集 activation >= 0.15 的节点到 active_ids
- 只对 active 节点做传播，避免遍历全部 ~23712 节点
- 在激活率 < 10% 时节省 ~90% 遍历

---

### P2-2：意图向量初始权重区分 ✅

**文件**：`src/cognitive_controller.c:55-62`

```c
// 原：均匀 0.1 × 10
// 改：词汇=0.20 语义=0.18 情绪=0.10 语法=0.10
//      上下文=0.12 领域=0.08 语用=0.07 文化=0.05 概念=0.10
```

词汇和语义拓扑作为回答生成的主要信号源获得更高初始权重。

---

### P2-3：global_visited 跨拓扑语义 ✅

**状态**：已验证无实际问题。各拓扑节点 ID 空间独立，visited 位图只覆盖当前子拓扑，不存在跨拓扑混淆。

---

### P2-1：拓扑边剪枝机制 ✅

**文件**：
- `src/trace_wisdom_topology.c` — 新增 `trace_wisdom_net_prune_edges(net, min_confidence, min_weight)`
- `src/multi_topology.c` — 新增 `master_prune_cross_links(master, min_weight, min_use_count)`
- `include/trace_wisdom_topology.h` + `include/multi_topology.h` — 声明

- 内部边：双阈值（confidence + weight）淘汰低质量边
- 跨拓扑连接：双阈值（weight + use_count）淘汰低频连接
- 跨拓扑剪枝不压缩数组（设 NULL），cross_adj 访问时检查有效性

---

### P3-3：备份文件清理 ✅

`src/` 目录中 `.codewhale` / `.bak` 文件已清理，仅保留 58 个干净 .c 文件。

### P3-2：跨平台注释 ✅

**文件**：`src/dialog_system.c:19-21`

```c
// 原：// #include "network_tool.h"  // Windows-only
// 改：#ifdef _WIN32 / #include "network_tool.h" / #endif
```

### P3-1：parallel_mode 死代码 ✅

**状态**：已验证。已改名为 `_pad_parallel_mode`，注释标注"二进制兼容 padding"。删除会破坏状态文件格式，不可删。

---

## 三、编译结果

✅ `make batch-learn` — 全量重编译，0 error 0 warning，`build/bin/batch_learn` 链接成功。

## 四、改动文件汇总

| 文件 | 改动项 |
|------|--------|
| `src/multi_topology.c` | P0 语义投票 cross_adj + P2-1 跨拓扑剪枝 |
| `src/pruning.c` | P1-4 quickselect ×4 |
| `src/trace_wisdom_topology.c` | P1-3 递归锁 + P2-1 内部边剪枝 |
| `src/cognitive_controller.c` | P1-2 bsearch + P2-2 意图权重 |
| `src/dialog_system.c` | P1-1 active_set + P3-2 跨平台 |
| `include/trace_wisdom_topology.h` | P2-1 声明 |
| `include/multi_topology.h` | P2-1 声明 |

## 五、后续建议

- 在实际训练中调 `trace_wisdom_net_prune_edges(net, 0.1f, 0.05f)` 和 `master_prune_cross_links(master, 0.1f, 3)` 参数
- 拓扑剪枝可集成到刷盘流程中（每 N 轮自动执行）
