# 修复报告 013-B — 集成缺口与一致性修复

> 配对审查报告：013-A
> 日期：2026-05-27

## 一、修复概要

按 013-A 审查结果，修复剪枝函数集成缺口、状态文件 NULL 槽位、权重一致性、重复 include 等 4 项问题。

## 二、已修复项

### P0-1：剪枝函数集成 ✅

**文件**：`src/autonomic_learner.c:166-174`

在 `autonomic_request_flush()` 刷盘前调用两个剪枝函数：

```c
// 刷盘前先剪枝低质量边（控制拓扑膨胀）
for (int t = 0; t < master->sub_topo_count; t++) {
    SubTopology* sub = master->sub_topologies[t];
    if (sub && sub->net && sub->net->node_count > 0) {
        trace_wisdom_net_prune_edges(sub->net, 0.05f, 0.02f);
    }
}
master_prune_cross_links(master, 0.05f, 2);
```

剪枝阈值：内部边 conf<0.05 且 |w|<0.02 删除；跨拓扑 w<0.05 且 use<2 删除。

---

### P0-2：master_save_state NULL 槽位修复 ✅

**文件**：`src/multi_topology.c:2129-2135`

```c
// 改前：fwrite(&cross_link_count, ...)  — 包含 NULL 槽位
// 改后：先统计 actual_cross_links，再写入
int actual_cross_links = 0;
for (int i = 0; i < master->cross_link_count; i++) {
    if (master->cross_links[i]) actual_cross_links++;
}
fwrite(&actual_cross_links, sizeof(int), 1, fp);
```

确保文件头中的连接数与实际写入的连接数一致。

---

### P1-1：get_intent_base_weights 默认分支同步 ✅

**文件**：`src/cognitive_controller.c:292-296`

```c
// 改前：均匀分布 base[i] = 0.1f
// 改后：偏重词汇/语义，与 cognitive_controller_create 一致
static const float default_base[MAX_SUBTOPOS] = {
    0.20f, 0.18f, 0.10f, 0.10f, 0.12f, 0.08f, 0.07f, 0.05f, 0.10f
};
memcpy(base, default_base, MAX_SUBTOPOS * sizeof(float));
```

---

### P1-2：dialog_system.c 重复 include 清理 ✅

**文件**：`src/dialog_system.c:27`

删除第二个 `#include "string_pool.h"`，保留 L18 的。

---

## 三、编译结果

✅ `make batch-learn` — 0 error 0 warning，`build/bin/batch_learn` 链接成功。

## 四、改动文件汇总

| 文件 | 改动项 |
|------|--------|
| `src/autonomic_learner.c` | P0-1 刷盘前剪枝 |
| `src/multi_topology.c` | P0-2 统计实际 link 数 |
| `src/cognitive_controller.c` | P1-1 默认分支权重同步 |
| `src/dialog_system.c` | P1-2 删除重复 include |
