# 089 — v0.6.4：批2「改口径」+ BG-09 虚词污染修复

> 版本 **v0.6.4** · 2026-09-16 · 分支 `feat/v064-b2-a38`（基点 `a196e98` = v0.6.3 线）
> 工作区 `/home/cx/pm-fix`（Pi 3B）· 编译验证：armbian-1（gcc 13.3.0）

---

## 一、核心变更

- **B 类判据收敛（B3，`c9f8ae0`）**：7 个进输出点的 B 类判据收敛为「单码点 ∧ 汉字」，
  修掉 v0.5.38 起登记的「按字节数猜汉字」老契约违例。判据足迹与旧行为逐点等价（子集分解 A/B 结论）。
- **BG-09 虚词污染修复（`c6d8eca`）**：修复虚词（功能词）在生成端输出里污染作答的问题。
- **A12 丘脑总线定向投递（`6d906f1` / `ae5be0a` / `de85b10`）**：8 处广播改为定向投递，
  止住「无主信号挤队列」；接通丘脑信号消费；修 `idle_ticks` 恒增的既有 bug；登记 9 项零投递信号。
- **A31 reach 合成式（`38c0ce5` / `3948ee7`）**：reach 合成式改为「出口效率为主」，修掉方向相反的激励；
  `GF_DEGEN_MU` 0.35 → 0.40，给反退化判据留余量。

## 二、来源 / 上下文

批2（B2）「改口径」：在 v0.6.3 走边生成·批1 收口后，继续把残留的「按字节长度猜汉字」判据换掉。
前置依赖是 PR #6「正梁」（`f73427d` 合进 v0.6.3 线）—— 无正梁则旧判据无法安全替换。

## 三、改动文件（27 文件，+750 / -57）

- 生成端：`src/diffusion.c`（+287）、`src/generation_feedback.c`（新）、`include/generation_feedback.h`（新）、
  `src/dialog_generate.c`、`src/prefrontal_executive.c`
- 丘脑：`src/thalamus.c`、`include/thalamus.h`、`demos/gateway_system.c`、`include/dialog_system.h`
- 拓扑 / reach：`src/multi_topology.c`、`include/multi_topology.h`、`include/diffusion.h`、`src/visual_cortex.c`、`src/media_reader.c`、`src/article_reader.c`、`src/cingulate.c`、`src/dialog_system.c`
- 版本：`include/pivotmind_version.h`

## 四、编译验证

- armbian-1（RK3399，gcc 13.3.0）全量 `make -j6 all` + `check-tools` + `check-version`
- WSL（x86_64，gcc 15.2.0）交叉覆盖

## 五、部署

- 线上仍是 v0.5.34；本次部署 = 换二进制 + 布局迁移（SSOT），不可逆窗口 ≈ 3 分钟。
- 阻塞点仍是「布局」不是「fmt_ver」：`fmt_ver=9` 状态在 SSOT 布局下新版本直接读得进。
